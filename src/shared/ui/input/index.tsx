import React, { FunctionComponent } from "react";
import { Input } from "./styles";

export const InputWidget: FunctionComponent = () => {
  return <Input type="text" placeholder="Введите задачу" />;
};
